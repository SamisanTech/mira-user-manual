
To install sphinx:

pip install sphinx

To install theme
pip install sphinx_rtd_theme

mkdir project-name
sphinx-quickstart

./make html (dot slash compulsory)

to open the build -> .\_build\html\index.html
